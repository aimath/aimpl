

<section>
  <title>The <m>u</m>-invariant problem</title>
  <p>
    The problems in this section are related to the u-invariant problem on the maximal dimension of an anisotropic quadratic form.
  </p>

  <openquestion>
    <statement>
      <p>
        If <m>u(F) \lt \infty</m>, does there exist <m>B \in \N</m> such that <m>u(F') \lt 
        \infty</m> for all <m>F'/F</m> finite? In particular, is <m>B = 2u(F)</m>
        sufficient?
      </p>
    </statement>
  </openquestion>

  <openquestion>
    <statement>
      <p>
        When is <m>\tau_F</m> finite?
      </p>
    </statement>
  </openquestion>

  <openproblem>
    <statement>
      <p>
        Find interesting classes <m>\mathcal F</m> of fields such that for <m>F \in
        \mathcal F</m>,
        <men>
          u(F) \lt \infty \Longrightarrow u(F(t)) \lt \infty.
        </men>
      </p>
    </statement>
    <suggestion>
      <p>
        Examples to try:
        <men>
          \mathcal F  = \{F \  \ :\ \   |F^{\times}/F^{\times 2}| \lt \infty \}
        </men>
        and
        <men>
          \mathcal F' = \{F \ \ : \ \  F^{\times} = F^{\times 2} \}
        </men>
      </p>
    </suggestion>
  </openproblem>

  <openquestion>
    <statement>
      <p>
        When <m>F = \R</m> , we have <m>u(F) = \infty</m> but <m>\tau_F \lt \infty</m>. Is there
        an example where this holds for <m>F</m> not formally real?
      </p>
    </statement>
  </openquestion>

  <openquestion>

    <introduction>
      <p>
        Define
        <men>
          u_k(F) = \max \{ \dim (q)\ \ |\ \ q \in I^k \}.
        </men>
        Then <m>u_0 = u</m>.
      </p>
    </introduction>

    <statement>
      <p>
        Can we compute <m>u_k(F)</m>? How about <m>u_3(F)</m>?
      </p>
    </statement>
    <status>
      <p>
        Note that forms in
        <m>I^3</m> have trivial Clifford invariant.
      </p>
    </status>
  </openquestion>

  <openquestion>
    <statement>
      <p>
        Find reasonable classes <m>\mathcal F</m> of fields such that <m>u(F)</m> is a
        power of 2 for all <m>F \in \mathcal F</m>. For these fields, assume <m>k</m> is the
        period-index bound for <m>l=2</m>. Does this imply <m>u(F) = 2^{k+1}</m>?
      </p>
    </statement>
    <opinion>
      <p>
        I think I've seen this somewhere before
      </p>
    </opinion>
  </openquestion>

  <openquestion>

    <introduction>
      <p>
        Let
        <men>
          \nu(F) = \max \{n\ |\ I^n \neq 0 \}.
        </men>
      </p>
    </introduction>

    <statement>
      <p>
        If <m>\nu(F) \lt \infty</m>, is it true that <m>\nu(F') \lt \nu(F) +1</m> for all
        <m>F'/F</m> finite?
      </p>
    </statement>
    <commentary>
      <p>
        <m>2^{\nu(F)} \lt u(F)</m>
      </p>
    </commentary>
    <context>
      <originator>
      <person aimplid="org.aimpl.user:c"/>
      </originator>

      <p>
        This is related to the computation of Galois groups of
        quadratically closed fields.
      </p>
    </context>
  </openquestion>

  <openquestion>

    <introduction>
      <p>
        For a quadratic form <m>q</m> over <m>F</m>, we define
        <men>
          \textrm{splitting degree} = \min\{[L:F]\ :\ q_{L}\ \textrm{is a
          direct sum of hyperbolics}\}.
        </men>
        The <term>torsion index</term> of <m>F</m>, denoted <m>\tau_F</m> is the maximum
        splitting degree, taken over all even dimensional <m>q</m>.
      </p>
    </introduction>

    <statement>
      <p>
        Is <m>\tau_F \lt 2^{(\frac{u(F)}{2} -1)}</m>?
      </p>
    </statement>
  </openquestion>
</section>

