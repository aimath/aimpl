

<section>
  <title>The Brauer group</title>
  <openquestion>
    <statement>
      <p>
        Fix <m>d \geq 3</m>. Assume that we know:

        <m>\operatorname{ind} (\alpha) |   \operatorname{Per}(\alpha)^{d-1}</m>, for all <m>\alpha \in \operatorname{Br}(X)</m>, for
        all smooth, projective <m>X</m> over <m>\overline{\mathbb{F}_p}</m>. (Here <m>d
        = \dim (X)</m>.)

        Can we use this, by a boundedness argument, to show
        the same thing for <m>\alpha \in \operatorname{Br}(Y)</m>, where <m>Y</m> smooth, projective,
        <m>\dim(Y) = d</m> over <m>\C</m>?
      </p>
    </statement>
  </openquestion>

  <openquestion>
    <statement>
      <p>
        Is every Brauer class over <m>\C(s,t)</m> cyclic?
      </p>
    </statement>
    <commentary>
      <p>
        This type of question does not reduce to the prime
        case.
      </p>
    </commentary>
  </openquestion>

  <openproblem>

    <introduction>
      <p>
        A variant of Problem 2.2:
      </p>
    </introduction>

    <statement>
      <p>
        Higher cohomology <m>H^d(F, \pmb{\mu}_n^{\otimes d})</m>,
        where <m>d = \dim (F)</m>.
      </p>
    </statement>
  </openproblem>

  <openquestion>
    <statement>
      <p>
        When is <m>\operatorname{Br}(X) = \operatorname{Br'}(X)</m>?
      </p>
    </statement>
    <status>
      <p>
        If <m>X</m> is quasi-proj, then yes.
      </p>
    </status>
  </openquestion>
</section>

